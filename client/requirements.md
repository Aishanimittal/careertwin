## Packages
recharts | Dashboard analytics and confidence visualization
framer-motion | Smooth page transitions and polished micro-interactions
lucide-react | Beautiful, consistent iconography
clsx | Conditional class name construction
tailwind-merge | Merging tailwind classes safely

## Notes
- Authentication relies on cookie-based session management (`credentials: 'include'`)
- Prediction API expects `cgpa` (number), `skills` (string array), and `interests` (string array)
- Custom typography 'Space Grotesk' used for display fonts, 'Inter' for body fonts.
